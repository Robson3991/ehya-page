import { css } from 'styled-components';

const variables = css`
  :root {
    --transition: 0.3s;
    --font-primary: 'Open Sans', sans-serif;
    --font-headings: 'HK Grotesk', sans-serif;
  }
`;

export default variables;
